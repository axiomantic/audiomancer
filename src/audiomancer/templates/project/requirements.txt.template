# Python dependencies for {{ project_name }}
#
# Install with: pip install -r requirements.txt
# Or with uv:   uv pip install -r requirements.txt

# AI-assisted music production (install from source if not on PyPI)
# pip install -e /path/to/audiomancer
# audiomancer

# Sample library management
aiofiles
tqdm>=4.66.0
rich

# Interactive TUI
questionary>=2.0.0

# Development: Type stubs
types-tqdm>=4.66.0
